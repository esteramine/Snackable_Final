<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.snackable">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Snackable">
        <activity android:name=".SettingsActivities.DisplayChoicesActivity"></activity>
        <activity android:name=".SettingsActivities.SortChoicesActivity"></activity>
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListsActivity.YourListActivity" />
        <activity android:name=".ScanActivity" />
        <activity android:name=".ProductDetailActivity.ProductItemDetail" />
        <activity android:name=".CompareActivity.CompareActivity"></activity>
        <activity android:name=".FoodAdditiveDetailActivity.FoodAdditiveDetailActivity" />
        <activity android:name=".MainActivity"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>